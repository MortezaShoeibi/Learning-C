#include <stdio.h>
#include <stdlib.h>

int main() {

    int first_number = 222;
    int second_number = 444;
    int evil_number = first_number + second_number;
    printf("The result is so Evil! it's: %d\n" ,evil_number);

    return 0;
}
